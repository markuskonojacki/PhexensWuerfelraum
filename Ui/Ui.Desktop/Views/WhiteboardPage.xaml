<Page x:Class="PhexensWuerfelraum.Ui.Desktop.WhiteboardPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:cefSharp="clr-namespace:CefSharp.Wpf;assembly=CefSharp.Wpf"
      xmlns:Controls="http://metro.mahapps.com/winfx/xaml/controls"
      mc:Ignorable="d"
      Style="{DynamicResource PageStyle}"
      Title="BattleMapPage"
      DataContext="{Binding Settings, Source={StaticResource Locator}}">

    <Grid>
        <Controls:ProgressRing Foreground="{DynamicResource AccentColorBrush}"/>
        <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0 100 0 0" FontWeight="Bold">Wenn hier nichts läd, stelle sicher, dass du die Einstellungen (Whiteboard URL) richtig ausgefüllt hast.</TextBlock>
        <cefSharp:ChromiumWebBrowser Grid.Row="0" Address="{Binding Setting.WhiteboardUrl}" />
    </Grid>
</Page>
